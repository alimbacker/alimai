# Server
PORT=4000
JWT_SECRET=change_this_to_a_long_random_string

# Model provider API keys — add whichever ones you want to enable.
# The app will work with just ONE of these; add more later to unlock more models.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
